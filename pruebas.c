#include <stdio.h>
#include <stdlib.h>
#include <string.h>
int main()
{
   int numero;
   char hola;
   printf("Hola");
   scanf("%s", &hola);
   printf("Calculando...");
   if(hola==0) {
       printf("No nada we");
        
        getch();
   }
   printf("Si hay wee");
   getch();
   return 0;
}
